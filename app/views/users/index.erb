<a href="/users/new">Register as new User...</a>

<% @users.each do |user_row| %>
  <div class="user">
    <h3><a href="/users/<%= user_row.id %>"><%= user_row.name %></a> <small>(<%= user_row.email %>)</small>
    </h3>
  </div>
<% end %>

<a href="/tracks">View list of songs...</a>